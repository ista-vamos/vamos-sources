@PACKAGE_INIT@

set( vamos-sources_BUILD_TYPE @CMAKE_BUILD_TYPE@ )
set( vamos-sources_C_COMPILER @CMAKE_C_COMPILER@ )
set( vamos-sources_CXX_COMPILER @CMAKE_CXX_COMPILER@ )
set( vamos-sources_BINARY_DIR @vamos-sources_BINARY_DIR@ )
set( vamos-sources_SOURCE_DIR @vamos-sources_BINARY_DIR@ )
set( vamos-sources_INCLUDE_DIRS "${CMAKE_CURRENT_LIST_DIR}/../../include" )
set( vamos-sources_INSTALL_PREFIX @CMAKE_INSTALL_PREFIX@ )
set( vamos-sources_OPTION_ENABLE_IPO @ENABLE_IPO@ )
set( vamos-sources_PYTHONPATH "@vamos-sources_SOURCE_DIR@/python" )

#include ( "${CMAKE_CURRENT_LIST_DIR}/vamos-sources.cmake" )

